type User {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
  name: String!
}

extend type Query {
  users: [User!]!
}

input NewUser {
  name: String!
}

extend type Mutation {
  createUser(input: NewUser!): User!
}
