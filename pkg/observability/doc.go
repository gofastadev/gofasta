// Package observability provides Prometheus metrics (request count, duration,
// in-flight requests) exposed at /metrics, and distributed tracing with
// OpenTelemetry. Both are available as HTTP middleware.
package observability
