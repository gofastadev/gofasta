// Package middleware is a collection of HTTP middleware: request logging,
// panic recovery, CORS, security headers (HSTS, CSP, X-Frame-Options), rate
// limiting, request ID generation, and content-type validation. Compose them
// with Chain().
package middleware
